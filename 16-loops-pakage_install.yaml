- name: install pakages
  hosts: web
  become: yes
  tasks:
  - name: install packages
    anisble.builtin.dnf:
      name: "{{ item }}"
      state: latest
    loop:
    - mysql
    - nginx
    - postfix