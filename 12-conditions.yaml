- name: conditions
  hosts: web
  become: yes
  tasks:
  - name: check user exist or not
    ansible.builtin.command: id expence
     
  - name: say hello
    ansible.builtin.debug:
      msg: "Hello"
      
